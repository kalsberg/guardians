services:
  api:
    build: .
    container_name: guardians_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/projects.db
    volumes:
      - ./data:/app/data
